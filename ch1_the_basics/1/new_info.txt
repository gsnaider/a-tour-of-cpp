object file

include vs namespace

curly-brace initializer

auto

const
    const vector<int>& vec --> can I change the values inside?
constexpr

different ways to init arrays.

references (int& param)
